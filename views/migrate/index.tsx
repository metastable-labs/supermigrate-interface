import { LangParamProp } from "@/config/internationalization/i18n";

const MigrateView = ({ lang }: LangParamProp) => {
  return (
    <div>
      <h1>Migrate</h1>
    </div>
  );
};

export default MigrateView;
